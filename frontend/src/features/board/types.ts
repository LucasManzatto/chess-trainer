export type BoardAnnotationArrow = { from_square: string; to_square: string; color: string; comment: string | null }
export type BoardAnnotationCircle = { square: string; color: string; comment: string | null }
